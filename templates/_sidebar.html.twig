{% set currentRoute = app.request.attributes.get('_route')|default('') %}

<aside id="ta-sidebar" class="ta-sidebar" data-mobile-nav-target="sidebar">
    <div class="ta-sidebar-inner">
        <div class="ta-sidebar-brand">
            <a href="{{ path('app_homepage') }}" class="ta-sidebar-logo-link" title="Accueil LPF'26">
                {% include '_lpf_brand_logo.html.twig' with { img_class: 'ta-sidebar-logo', front_logo: true } %}
            </a>
        </div>

        <nav class="ta-sidebar-nav" aria-label="Navigation principale">
            <ul class="menu w-full p-0">
                <li>
                    <a href="{{ path('app_homepage') }}" class="ta-menu-link {{ currentRoute == 'app_homepage' ? 'active' : '' }}">
                        <i class="ti ti-home" aria-hidden="true"></i>
                        Accueil
                    </a>
                </li>
                <li>
                    <a href="{{ path('app_matches') }}" class="ta-menu-link {{ currentRoute in ['app_matches', 'app_pronostics'] ? 'active' : '' }}">
                        <i class="ti ti-ball-football" aria-hidden="true"></i>
                        Matchs
                    </a>
                </li>
                <li>
                    <a href="{{ path('app_groups') }}" class="ta-menu-link {{ currentRoute == 'app_groups' ? 'active' : '' }}">
                        <i class="ti ti-flag" aria-hidden="true"></i>
                        Groupes
                    </a>
                </li>
                <li>
                    <a href="{{ path('app_knockout') }}" class="ta-menu-link {{ currentRoute == 'app_knockout' ? 'active' : '' }}">
                        <i class="ti ti-tournament" aria-hidden="true"></i>
                        Phases finales
                    </a>
                </li>
                <li>
                    <a href="{{ path('app_ranking') }}" class="ta-menu-link {{ currentRoute == 'app_ranking' ? 'active' : '' }}">
                        <i class="ti ti-trophy" aria-hidden="true"></i>
                        Classement
                    </a>
                </li>
                <li>
                    <a href="{{ path('app_jokers') }}" class="ta-menu-link {{ currentRoute == 'app_jokers' ? 'active' : '' }}">
                        <i class="ti ti-wand" aria-hidden="true"></i>
                        Jokers
                    </a>
                </li>
                <li>
                    <a href="{{ path('app_forum') }}" class="ta-menu-link {{ currentRoute in ['app_forum', 'app_forum_reply'] ? 'active' : '' }}">
                        <i class="ti ti-messages" aria-hidden="true"></i>
                        Forum
                    </a>
                </li>
                <li>
                    <a href="{{ path('app_reglement') }}" class="ta-menu-link {{ currentRoute == 'app_reglement' ? 'active' : '' }}">
                        <i class="ti ti-file-text" aria-hidden="true"></i>
                        Règlement
                    </a>
                </li>
                <li>
                    <a href="{{ path('app_account') }}" class="ta-menu-link {{ currentRoute == 'app_account' ? 'active' : '' }}">
                        <i class="ti ti-user" aria-hidden="true"></i>
                        Compte et équipe
                    </a>
                </li>
            </ul>
        </nav>

        <div class="ta-sidebar-footer">
            {% include '_sidebar_user.html.twig' %}
            <div class="ta-sidebar-actions">
                <button
                    type="button"
                    class="ta-sidebar-notify site-intro-trigger"
                    data-site-intro-open
                    title="Découvrir le site"
                    aria-label="Découvrir le site — présentation"
                >
                    <i class="ti ti-player-play" aria-hidden="true"></i>
                </button>
                {% include '_notification_bell.html.twig' %}
                {% include '_site_theme_toggle.html.twig' %}
                {% if is_granted('ROLE_ADMIN') %}
                    {% if app.environment != 'prod' %}
                        <a
                            href="{{ path('admin_joker_test_scenario') }}"
                            class="ta-sidebar-notify ta-sidebar-jokers{{ currentRoute == 'admin_joker_test_scenario' ? ' ta-sidebar-notify--current' : '' }}"
                            title="Scénario jokers"
                            aria-label="Scénario jokers"
                        >
                            <i class="ti ti-mask" aria-hidden="true"></i>
                        </a>
                    {% endif %}
                    <a
                        href="{{ path('admin') }}"
                        class="ta-sidebar-notify ta-sidebar-admin{% if app.environment == 'prod' %}{{ currentRoute starts with 'admin' ? ' ta-sidebar-notify--current' : '' }}{% else %}{{ currentRoute starts with 'admin' and currentRoute != 'admin_qa_checklist' and currentRoute != 'admin_compet_checklist' and currentRoute != 'admin_joker_test_scenario' ? ' ta-sidebar-notify--current' : '' }}{% endif %}"
                        title="Administration"
                        aria-label="Administration"
                    >
                        <i class="ti ti-settings" aria-hidden="true"></i>
                    </a>
                {% endif %}
                <form method="post" action="{{ path('app_logout') }}" class="ta-sidebar-logout-form" data-turbo="false">
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('logout') }}">
                    <button
                        type="submit"
                        class="ta-sidebar-notify ta-sidebar-logout"
                        title="Déconnexion"
                        aria-label="Déconnexion"
                    >
                        <i class="ti ti-logout" aria-hidden="true"></i>
                    </button>
                </form>
            </div>
        </div>
    </div>
</aside>
