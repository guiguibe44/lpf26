{# Dev local uniquement — tester l’animation de déblocage badge #}
<section class="badge-dev-simulate ta-card mt-5 p-4">
    <div class="badge-dev-simulate__head">
        <div>
            <h2 class="text-base font-semibold mb-1">Badges — outil dev</h2>
            <p class="ta-card-text mb-0 text-sm">
                Simule l’obtention d’un badge et l’animation vers le menu « Compte et équipe ».
            </p>
        </div>
        <button type="button" class="btn btn-warning btn-sm" data-badge-simulate-unlock>
            <i class="ti ti-sparkles" aria-hidden="true"></i>
            Simuler un nouveau badge
        </button>
    </div>
</section>
