# Copier vers test-match-scenario.env et renseigner après création du match / buteurs.
# source scripts/test-match-scenario.env

export MATCH_ID=0
export BUTEUR_FR_1=0
export BUTEUR_DE_1=0
export BUTEUR_FR_2=0

# Prod — cron-job.org (URLs : ./scripts/print-test-match-cron-urls.sh après export CRON_SECRET)
# export BASE_URL=https://26.lotopotofoot.fr
# Ne pas versionner le secret : export CRON_SECRET='…' dans le shell uniquement.
